;; Install Makefile.cfml in <opam root>/lib/cfml.
(install
  (package cfml)
  (section lib)
  (files Makefile.cfml)
)
;; Install ocamldep.post in <opam root>/lib/cfml,
;; with the executable bit set.
(install
  (package cfml)
  (section libexec)
  (files ocamldep.post)
)
